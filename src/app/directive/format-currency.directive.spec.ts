import { FormatCurrencyDirective } from './format-currency.directive';

describe('FormatCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
